const Unit = {};
const string_eq = (a, b) => a === b;
const prim__list_length = (l) => l.length;
const prim__int_eq = (a, b) => a === b;
const prim__list_make = (len, f) => Array.from({length: len}, (_, i) => f(i));
const prim__int_sub = (a, b) => a - b;
const prim__list_get = (l, i) => l[i];
const prim__int_add = (a, b) => a + b;
const ParseResult = (result, rest) => ({result, rest});
const CST = {Error: (msg, span) => ({type: "Error", msg, span}), SeqOf: (acc, span) => ({type: "SeqOf", acc, span})};
const Span = (start, end) => ({start, end});
const list_filter = (l, f) => l.filter(f);
const lex = (s) => [{kind: "Whitespace"}, {kind: "Id", text: "let"}];
(() => { Unit; enum(SplitResult, (() => { case(Found(before(:, List(CST)), after(:, List(CST)))); case(NotFound); return Unit; })()); function split_at_eq(acc, ls) { (() => { if (prim__int_eq(prim__list_length(ls), 0)) { return SplitResult.NotFound; } else { const x = prim__list_get(ls, 0); const rest = advance(ls); (() => { const _match_val = x; throw new Error('Non-exhaustive match'); })(); return Unit; }})(); return Unit; }function collapse_apps(elems, acc) { (() => { if (prim__int_eq(prim__list_length(elems), 0)) { return acc; } else { const x = prim__list_get(elems, 0); const rest = advance(elems); (() => { const _match_val = x; throw new Error('Non-exhaustive match'); })(); return Unit; }})(); return Unit; }function expand_if(elems, span) { Unit; CST.Error("Not implemented expand_if", span); return Unit; }function expand_seq_expr(elems, span) { const collapsed = collapse_apps(); const len = prim__list_length(collapsed); (() => { if (prim__int_eq(len, 0)) { return CST.Tuple(list_empty(CST)(), span); } else { if (prim__int_eq(len, 0)) { return prim__list_get(collapsed, 0); } else { const first = prim__list_get(collapsed, 0); (() => { const _match_val = first; throw new Error('Non-exhaustive match'); })(); return Unit; }}})(); return Unit; }function process_stmts(ss) { (() => { if (prim__int_eq(prim__list_length(ss), 0)) { return ss; } else { const s = prim__list_get(ss, 0); const rest = process_stmts(advance(ss)); (() => { const _match_val = s; throw new Error('Non-exhaustive match'); })(); return Unit; }})(); return Unit; }function expand_cst(c) { (() => { const _match_val = c; throw new Error('Non-exhaustive match'); })(); return Unit; }function expander_main() { const cst = CST.Symbol("test", Span(0, 0)); expand_cst(cst); return Unit; }expander_main(); return Unit; })(); 