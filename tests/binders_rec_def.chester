// Recursive def refers to itself by name (global binder).
// Expect: 6
def fact(n: Integer): Integer = {
  if int_eq(n, 0) then 1 else int_mul(n, fact(int_sub(n, 1)))
};
def main(): Integer = fact(3);
