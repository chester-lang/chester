signature HasId {
  type t;
  def id(x: t): t
};

module NatId : HasId {
  type t = Int;
  def id(x: t): t = { x }
};

module OpaqueId :> HasId {
  type t = Int;
  def id(x: t): t = { x }
};

def main(): Unit = {
  let _ = NatId.id(1);
  let _ = OpaqueId.id(2);
  ()
}
