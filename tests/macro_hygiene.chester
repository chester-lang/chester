// Macro-introduced `tmp` must not overwrite the caller's `tmp`.
// Expect: 42
macro swap {
  case (a, b) => {
    let tmp = a;
    let a = b;
    let b = tmp
  }
};
def main(): Integer = {
  let tmp = 42;
  let a = 1;
  let b = 2;
  swap(a, b);
  tmp
};
