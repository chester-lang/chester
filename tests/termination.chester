// This tests Koka-style implicit 'div' effect inference for recursive functions.
// 'loop' is directly recursive, so it gets the 'div' effect automatically.

def loop(n: Integer): Integer = {
  if int_eq(n, 0) then 0 else loop(int_sub(n, 1))
};

def main(): Integer = {
  loop(5)
};
