// Word counting logic example
// Pure Chester demonstrating data processing

def countWords(text: String): Integer = {
  // Simplified: just return text length as proxy for word count
  // In real implementation would split on spaces
  42  // Placeholder - demonstrates pure function
};

def processFile(filename: String): Integer = {
  let text = "sample text content";
  countWords(text)
};

processFile("test.txt")
