// Ported from chester2026draft/examples/go/go-simple.chester (add(40, 2)).
// Uses int_add because infix + desugaring lives in self-hosted expander only.

def add(x: Integer, y: Integer): Integer = {
  int_add(x, y)
};

def main(): Integer = {
  add(40, 2)
}
